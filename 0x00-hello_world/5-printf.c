#include <stdio.h>

/**
 * main - main this function prints exactly with proper grammar
 *
 * Return: 0 when successfull
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
